# secrets — never commit the real token
.env

# fetched Instagram images + manifest (regenerated by the build script)
assets/ig/
assets/instagram.json

# OS / editor cruft
.DS_Store
Thumbs.db
node_modules/
