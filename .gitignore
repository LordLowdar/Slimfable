# secrets — never commit the real token
.env

# Note: assets/ig/ and assets/instagram.json ARE committed — GitHub Pages
# serves the repo directly, so fetched gallery assets must live in it.

# OS / editor cruft
.DS_Store
Thumbs.db
node_modules/
