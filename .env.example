# Copy this file to ".env" and fill in the values. Never commit .env.

# Long-lived Instagram access token (from the Instagram API with Instagram Login flow).
# Get it once via the Meta app OAuth flow, then this script auto-refreshes it.
IG_ACCESS_TOKEN=

# How many recent posts to pull into the gallery (optional, default 12).
IG_LIMIT=12
